<% if product.published? %>
  <%= link_to 'Unpublush', product_unpublish_path(product), class: 'link', data: { 'turbo-method': :put } %>
<% else %>
  <%= link_to 'Publish', product_publish_path(product), class: 'link', data: { 'turbo-method': :put } %> 
<% end %>